name: Rust CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'crates/**'
      - 'examples/**'
      - 'tests/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/rust-ci.yaml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'crates/**'
      - 'examples/**'
      - 'tests/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/rust-ci.yaml'

concurrency:
  group: rust-ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # Format check (runs once for entire workspace)
  format:
    name: Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: "1.90"
          components: rustfmt
          profile: minimal
          cache-shared-key: rust-ci-1.90
          cache-workspace-crates: true
      
      - name: Check formatting
        run: cargo fmt --all -- --check

  # Per-crate quality checks (parallel)
  crate-checks:
    name: ${{ matrix.crate }} - Quality
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        crate:
          - gear-mesh
          - gear-mesh-core
          - gear-mesh-derive
          - gear-mesh-generator
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: "1.90"
          components: clippy
          profile: minimal
          cache-shared-key: rust-ci-1.90
          cache-key: ${{ matrix.crate }}
          cache-workspace-crates: true
      
      - name: Clippy
        run: cargo clippy --package ${{ matrix.crate }} --all-targets --all-features -- -D warnings
      
      - name: Test
        run: cargo test --package ${{ matrix.crate }} --all-features
      
      - name: Doc
        run: cargo doc --package ${{ matrix.crate }} --no-deps --all-features
        env:
          RUSTDOCFLAGS: -D warnings

  # Integration tests (runs once after crate checks)
  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: "1.90"
          profile: minimal
          cache-shared-key: rust-ci-1.90
          cache-key: integration
          cache-workspace-crates: true
      
      - name: Run all tests
        run: cargo test --workspace --all-features

  # Security audit
  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: "1.90"
          profile: minimal
          cache-shared-key: rust-ci-1.90
          cache-key: security
      
      - name: Install cargo-audit
        run: cargo install cargo-audit
      
      - name: Run security audit
        run: cargo audit

  # Dependency checks
  dependencies:
    name: Dependency Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: "1.90"
          profile: minimal
          cache-shared-key: rust-ci-1.90
          cache-key: dependencies
      
      - name: Install cargo-deny
        run: cargo install cargo-deny
      
      - name: Check licenses and advisories
        run: cargo deny check
